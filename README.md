## Data Source

This project uses FDA Adverse Event Reporting System (FAERS) ASCII quarterly data files.

The raw data files are not uploaded to this GitHub repository because they are large.  
Instead, the data can be downloaded directly from the FDA FAERS website.

## How to Download and Extract Data

Run the following command from the repository root:

```bash
python src/download_extract_faers.py